{
  "name": "nexor-ai",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "npm run dev --prefix web",
    "build": "npm run build --prefix web",
    "preview": "npm run preview --prefix web",
    "deploy": "cd gateway-worker && npx wrangler deploy"
  },
  "workspaces": [
    "web"
  ]
}
